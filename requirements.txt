# Scout Code Navigator - Dependencies
# Lightweight: no ML models, no vector DB

# MCP (Model Context Protocol) - Claude integration
mcp>=1.0.0

# HTTP Server (optional, for network mode)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Data validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Environment configuration
python-dotenv>=1.0.0
